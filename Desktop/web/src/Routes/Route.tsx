import React, {useContext} from 'react'
import {RouteProps as ReactDOMRouteProps, Route as ReactDOMRoute, Redirect} from 'react-router-dom'

import AuthContext from '../Context/auth'

interface RouteProps extends 